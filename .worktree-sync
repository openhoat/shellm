# Worktree Sync Configuration
# Files to copy from main worktree to new worktrees
# One file per line, comments start with #

# Environment configuration (direnv)
.envrc

# Environment variables (local overrides)
.env.local
.env.development.local

# Claude Code local settings
.claude/settings.local.json